# -*- mode: snippet -*-
# name: graph.weighted.adj
# key: graph.weighted.adj
# --

void addEdge(vector <pair<int, int> > adj[],
             int u,
             int v,
             int wt)
{
    adj[u].push_back(make_pair(v, wt));
    adj[v].push_back(make_pair(u, wt));
}
